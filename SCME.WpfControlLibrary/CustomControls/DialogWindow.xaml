<Window x:Class="SCME.WpfControlLibrary.CustomControls.DialogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:SCME.WpfControlLibrary.CustomControls"
        xmlns:res="clr-namespace:SCME.WpfControlLibrary.Properties"
        DataContext="{Binding RelativeSource={RelativeSource Self}}" Loaded="DialogWindow_OnLoaded"
        mc:Ignorable="d" WindowStyle="None"  SizeToContent="WidthAndHeight"
        Height="450" Width="800" MaxWidth="1280" MaxHeight="1024" d:DataContext="{d:DesignInstance local:DialogWindow}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition  Height="Auto" />
            <RowDefinition  Height="*" />
            <RowDefinition  Height="Auto" />
        </Grid.RowDefinitions>
        <Label Height="50" Grid.Column="0" Grid.Row="0" Content="{Binding Title}"/>
        <TextBlock Grid.Column="0" Grid.Row="1" Text="{Binding Message}" TextWrapping="WrapWithOverflow"/> 
        <Button Grid.Column="0" Grid.Row="2" Content="{x:Static res:Resources.Ok}" Command="{Binding CloseCommand}" CommandParameter="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=Window}}"/>
    </Grid>
</Window>
