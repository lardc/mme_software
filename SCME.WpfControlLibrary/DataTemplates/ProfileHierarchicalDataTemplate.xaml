<UserControl x:Class="SCME.WpfControlLibrary.DataTemplates.ProfileHierarchicalDataTemplate"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:convertes="clr-namespace:SCME.WpfControlLibrary.IValueConverters"
             xmlns:VM="clr-namespace:SCME.WpfControlLibrary.ViewModels"
             mc:Ignorable="d" d:DesignHeight="50" d:DesignWidth="200">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Column="0" Grid.Row="0" Text="{Binding Name}"/>
        <TextBlock Grid.Column="1" Grid.Row="0" Text="{Binding Version}"  Margin="{x:Static VM:ResourceBinding.MarginLeft05}"/>
            <TextBlock Grid.Column="2" Grid.Row="0" Text="{Binding Timestamp, StringFormat=hh:mm:ss dd-MM-yyyy}" TextWrapping="Wrap"  Margin="{x:Static VM:ResourceBinding.MarginLeft05}" HorizontalAlignment="Right"/>

    </Grid>
</UserControl>
